% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{predict_surface_batch}
\alias{predict_surface_batch}
\title{Predict pollutant surfaces for a batch of day/night columns}
\usage{
predict_surface_batch(
  data,
  targets,
  newgrid,
  algorithm = c("uk", "gam"),
  ...,
  overrides = list(),
  progress = TRUE
)
}
\arguments{
\item{data}{An \code{sf} POINT layer with \code{X}, \code{Y} columns and one response
column per element of \code{targets}.}

\item{targets}{Character vector of response columns (typically the
output of \code{\link[=decade_columns]{decade_columns()}}).}

\item{newgrid}{Output of \code{\link[=build_prediction_grid]{build_prediction_grid()}} (a \link[terra:SpatRaster-class]{terra::SpatRaster}).}

\item{algorithm}{\code{"uk"} or \code{"gam"}. (Renamed from \code{method} to avoid a
clash with \code{mgcv::gam()}'s own \code{method} argument when forwarded via
\code{...}; e.g. \verb{algorithm = "gam", method = "REML"} is now legal.)}

\item{...}{Extra arguments passed to \code{\link[=fit_uk]{fit_uk()}} or \code{\link[=fit_gam]{fit_gam()}}.}

\item{overrides}{Optional named list mapping \code{target} -> list of
argument overrides, used to reproduce the per-day variogram tweaks
in the legacy scripts. For example
\code{list(pm10_1_02_day = list(model = "Sph", psill = 40))}.}

\item{progress}{Logical; show a \code{cli} progress bar.}
}
\value{
A list with elements \code{pred} (\link[terra:SpatRaster-class]{terra::SpatRaster} of predictions,
one layer per target), \code{var} (kriging variance, NULL for GAM),
\code{models} (list of fitted model objects), and \code{diagnostics} (tibble
with \code{target}, \code{n_obs}, \code{mean}, \code{sd}, \code{nugget}, \code{psill}, \code{range}).
}
\description{
Loops \code{\link[=fit_uk]{fit_uk()}} or \code{\link[=fit_gam]{fit_gam()}} over a vector of target columns and
returns a multi-layer \link[terra:SpatRaster-class]{terra::SpatRaster} of predictions plus a
tibble of per-column diagnostics.
}
