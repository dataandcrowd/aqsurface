% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uk.R
\name{fit_uk}
\alias{fit_uk}
\title{Fit a universal kriging model for one pollutant column}
\usage{
fit_uk(
  data,
  target,
  trend = c("const", "coords"),
  cutoff = 30000,
  width = 3000,
  model = "Ste",
  psill = NULL,
  nugget = NULL,
  range = NULL,
  fit_kappa = TRUE,
  fit_method = 6,
  crs = 5181
)
}
\arguments{
\item{data}{An \code{sf} POINT layer or \code{data.frame} with \code{X}, \code{Y} columns
plus the response column.}

\item{target}{Character; name of the response column.}

\item{trend}{Trend specification. Either \code{"const"} (ordinary kriging,
\code{~ 1}), \code{"coords"} (universal kriging on coordinates, \code{~ X + Y}), or a
one-sided formula such as \code{~ DEM + Road_Dist} (kriging with external
drift).}

\item{cutoff, width}{Empirical variogram cutoff and bin width, in metres.}

\item{model}{Initial variogram family for \code{\link[gstat:vgm]{gstat::vgm()}}. Default
\code{"Ste"} (Matern with Stein parameterisation).}

\item{psill, nugget, range}{Optional starting values; if \code{NULL},
\code{gstat}'s defaults are used.}

\item{fit_kappa, fit_method}{Forwarded to \code{\link[gstat:fit.variogram]{gstat::fit.variogram()}}.}

\item{crs}{CRS for the response data when \code{data} is a plain
\code{data.frame}. Ignored if \code{data} is already \code{sf}.}
}
\value{
A list with class \code{"aqs_uk"} containing the empirical
variogram (\code{emp}), the fitted model (\code{model}), the formula,
the training data as \code{sf}, and the call.
}
\description{
Wraps \code{\link[gstat:variogram]{gstat::variogram()}} and \code{\link[gstat:fit.variogram]{gstat::fit.variogram()}} with a small set
of sensible defaults and a fallback strategy for the cases where
automatic Stein-Matern fits collapse on small Korean monitoring
networks. The companion legacy scripts hand-tuned variogram families
and starting values for problematic days; this function exposes those
overrides as arguments so the choices can be inspected and version
controlled.
}
