% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{extract_at_stations}
\alias{extract_at_stations}
\title{Extract pollutant predictions and observations at monitoring stations}
\usage{
extract_at_stations(
  surface,
  stations,
  targets,
  obs_df = NULL,
  station_id = NULL
)
}
\arguments{
\item{surface}{Multi-layer \link[terra:SpatRaster-class]{terra::SpatRaster} of predictions.}

\item{stations}{An \code{sf} POINT layer of monitoring sites (training or
held-out). Must carry the response columns when \code{obs_df} is \code{NULL}.}

\item{targets}{Character vector of target column names.}

\item{obs_df}{Optional long-format \code{data.frame} with columns
\code{Station}, \code{target}, \code{obs}. If supplied, observations are taken
from this table.}

\item{station_id}{Column in \code{stations} to use as a station identifier.
Defaults to \code{NULL}, which auto-detects the first available among
\code{"Station.ID"}, \code{"Station"}, \code{"station_id"}, and \code{"ID"}.}
}
\value{
A tibble with columns
\code{station}, \code{target}, \code{obs}, \code{pred}, and (if present in \code{stations})
\code{station_type}.
}
\description{
Companion to \code{\link[=predict_surface_batch]{predict_surface_batch()}} and \code{\link[=apply_road_ratio]{apply_road_ratio()}}.
Returns a long-format tibble with one row per (station, target),
carrying both observed and predicted values, ready for
\code{\link[=compute_metrics]{compute_metrics()}} or \code{ggplot2}. By default, observations are taken
from the same station \code{sf} (assumes the response columns are present);
if not, supply \code{obs_df} explicitly.
}
