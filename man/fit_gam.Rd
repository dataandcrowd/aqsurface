% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gam.R
\name{fit_gam}
\alias{fit_gam}
\title{Fit a thin-plate / tensor-product GAM surface for one pollutant column}
\usage{
fit_gam(
  data,
  target,
  smooth = c("tp", "te"),
  k = NULL,
  method = "REML",
  extra_terms = NULL
)
}
\arguments{
\item{data}{An \code{sf} POINT layer or \code{data.frame} with \code{X}, \code{Y} columns
plus the response.}

\item{target}{Character; name of the response column.}

\item{smooth}{\code{"tp"} for an isotropic thin-plate spline (\code{s(X, Y)}),
\code{"te"} for a tensor-product smooth (\code{te(X, Y)}).}

\item{k}{Optional basis dimension; passed to the smooth.}

\item{method}{Smoothing parameter selection method.}

\item{extra_terms}{Optional character vector of additional terms to
add to the right-hand side, e.g. \code{c("Road_Dist", "DEM")}.}
}
\value{
A list with class \code{"aqs_gam"} containing the fitted \code{gam}
object, the formula, the training data and the call.
}
\description{
Wraps \code{\link[mgcv:gam]{mgcv::gam()}} for the standard \code{y ~ s(X, Y)} (or \code{te(X, Y)})
model used in the legacy scripts. The estimation method defaults to
REML, which the original code switched to ML on a few days; in our
experience REML is more stable for the small Korean monitoring sample
and is the modern recommendation.
}
