% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{aqs_data_dir}
\alias{aqs_data_dir}
\title{Locate the legacy data directory}
\usage{
aqs_data_dir(path = NULL, required = TRUE)
}
\arguments{
\item{path}{Optional explicit path. When supplied it is returned
unchanged (after a \code{dir.exists()} check).}

\item{required}{Logical; if \code{TRUE} (default) and no path resolves,
throw an error rather than returning \code{NA}.}
}
\value{
A character path, or \code{NA_character_} when \code{required = FALSE}
and nothing resolves.
}
\description{
The raw \code{pm10.RData}, \code{no2.RData}, and \code{stations_10km.shp} files are
bulky and remain outside the repository (in OneDrive, on a shared
network drive, etc.). This helper resolves a single directory path
from, in order:
\enumerate{
\item the \code{path} argument if non-\code{NULL},
\item the \code{AQSURFACE_DATA_DIR} environment variable,
\item the \code{aqsurface.data_dir} R option.
}
}
\details{
Add a line to \verb{~/.Renviron} such as
\verb{AQSURFACE_DATA_DIR=/Users/you/OneDrive/.../Code/Data} so the path is
picked up automatically in every R session.
}
\examples{
\dontrun{
  Sys.setenv(AQSURFACE_DATA_DIR = "~/OneDrive/.../Code/Data")
  aqs_data_dir()
}
}
