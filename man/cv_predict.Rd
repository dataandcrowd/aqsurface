% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{cv_predict}
\alias{cv_predict}
\title{Run cross-validated predictions for a single target column}
\usage{
cv_predict(
  data,
  target,
  folds,
  algorithm = c("uk", "gam"),
  ...,
  station_id = NULL,
  quiet = TRUE
)
}
\arguments{
\item{data}{An \code{sf} POINT layer with \code{X}, \code{Y} columns and the
response column.}

\item{target}{Character; response column name.}

\item{folds}{Output of \code{\link[=spatial_kmeans_folds]{spatial_kmeans_folds()}} or
\code{\link[=spatial_block_folds]{spatial_block_folds()}}.}

\item{algorithm}{\code{"uk"} or \code{"gam"}.}

\item{...}{Extra arguments passed to \code{\link[=fit_uk]{fit_uk()}} or \code{\link[=fit_gam]{fit_gam()}}.}

\item{station_id}{Identifier column on \code{data}. Auto-detected if
\code{NULL} (see \code{\link[=extract_at_stations]{extract_at_stations()}}).}

\item{quiet}{Suppress per-fit warnings (recommended in benchmarks).}
}
\value{
Tibble with columns \code{station}, \code{target}, \code{obs}, \code{pred},
\code{fold}.
}
\description{
For each fold, fits the requested algorithm on the training
subset, predicts at the held-out monitoring locations, and
collects the (observation, prediction) pairs into a long-format
tibble suitable for \code{\link[=compute_metrics]{compute_metrics()}}.
}
\details{
Predictions are evaluated \strong{directly at the test station
locations} (\code{gstat::krige} and \code{predict.gam} both accept point
inputs), so no raster is built per fold; this is dramatically
faster than the surface-then-extract path.
}
