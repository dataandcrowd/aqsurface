% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine.R
\name{combine_stations}
\alias{combine_stations}
\title{Combine training and held-out monitors with a \code{station_type} factor}
\usage{
combine_stations(train, holdout, tag = c("fixed", "road"))
}
\arguments{
\item{train}{An \code{sf} POINT layer of training monitors.}

\item{holdout}{An \code{sf} POINT layer of held-out monitors.}

\item{tag}{Character of length 2: factor labels to attach to
\code{train} and \code{holdout} rows respectively. Defaults to
\code{c("fixed", "road")}.}
}
\value{
An \code{sf} POINT layer with the union of rows and a
\code{station_type} factor column.
}
\description{
Stacks two \code{sf} POINT layers (typically \code{train} = Fixed monitors
and \code{holdout} = Road monitors) into a single layer ready for
covariate-driven modelling. A \code{station_type} factor column is
added with levels \code{"fixed"} and \code{"road"}, so it can be used as a
covariate by \code{fit_uk(trend = ~ station_type)},
\code{fit_gam(extra_terms = "station_type")}, or
\code{fit_rf(predictors = c("X", "Y", "station_type", ...))}.
}
\details{
Only columns common to both layers are retained, so this is also
a way to drop attributes that exist in only one of the input
tables.
}
