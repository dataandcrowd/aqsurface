% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rf.R
\name{fit_rf}
\alias{fit_rf}
\title{Fit a random forest baseline for one pollutant column}
\usage{
fit_rf(
  data,
  target,
  predictors = c("X", "Y"),
  num_trees = 500,
  respect_unordered_factors = "order",
  ...
)
}
\arguments{
\item{data}{An \code{sf} POINT layer or \code{data.frame} with the response
and predictor columns.}

\item{target}{Character; response column name.}

\item{predictors}{Character vector of predictor columns. Defaults
to \code{c("X", "Y")}. Pass e.g.
\code{c("X", "Y", "Road_Dist", "DEM", "station_type")} to enable
covariate-driven RF.}

\item{num_trees}{Number of trees.}

\item{respect_unordered_factors}{\code{ranger} argument; default
\code{"order"} for compact handling of factor predictors.}

\item{...}{Extra arguments forwarded to \code{\link[ranger:ranger]{ranger::ranger()}}.}
}
\value{
A list with class \code{"aqs_rf"} containing the fitted ranger
object, the predictor names, the training data and the call.
}
\description{
Fast tree-based regression baseline using \code{\link[ranger:ranger]{ranger::ranger()}}.
Provides a reference point against which UK and GAM are compared in
the planned R Journal article. RF naturally handles non-linear
interactions between coordinates and station-level covariates
(\code{Road_Dist}, \code{DEM}, \code{station_type}), so it is the most
appropriate "modern ML" benchmark for the comparison.
}
