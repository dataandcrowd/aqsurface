% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_methods}
\alias{benchmark_methods}
\title{Benchmark interpolation methods across validation strategies}
\usage{
benchmark_methods(
  train,
  holdout = NULL,
  targets,
  algorithms = c("uk", "gam"),
  uk_args = list(trend = "const", cutoff = 30000, width = 3000, model = "Ste"),
  gam_args = list(smooth = "tp", method = "REML"),
  cv_folds = 5,
  fold_method = c("kmeans", "block"),
  block_size_m = 5000,
  seed = 1L,
  progress = TRUE
)
}
\arguments{
\item{train}{An \code{sf} POINT layer of training monitors carrying
the response columns.}

\item{holdout}{An \code{sf} POINT layer of held-out monitors (typically
road-side). Pass \code{NULL} to skip the held-out strategy.}

\item{targets}{Character vector of response columns (typically
the output of \code{\link[=decade_columns]{decade_columns()}}).}

\item{algorithms}{Character vector; subset of \code{c("uk", "gam")}.}

\item{uk_args}{A named list of extra arguments forwarded to
\code{\link[=fit_uk]{fit_uk()}} (e.g. \code{list(trend = "const", cutoff = 30000)}).}

\item{gam_args}{A named list of extra arguments forwarded to
\code{\link[=fit_gam]{fit_gam()}} (e.g. \code{list(smooth = "tp", method = "REML")}).}

\item{cv_folds}{Number of spatial folds.}

\item{fold_method}{Either \code{"kmeans"} (default; recommended) or
\code{"block"}. Forwarded to \code{\link[=spatial_kmeans_folds]{spatial_kmeans_folds()}} /
\code{\link[=spatial_block_folds]{spatial_block_folds()}}.}

\item{block_size_m}{Block size when \code{fold_method = "block"}.}

\item{seed}{Optional integer; sets the seed before fold creation
so the benchmark is reproducible.}

\item{progress}{Show a progress bar.}
}
\value{
A tibble with one row per (\code{algorithm}, \code{strategy},
\code{target}) combination, plus a final \code{overall} row per
(\code{algorithm}, \code{strategy}) collapsing across targets. Columns:
\code{algorithm}, \code{strategy}, \code{target}, \code{n}, \code{rmse}, \code{mae}, \code{bias},
\code{r}.
}
\description{
One-call wrapper that runs every requested algorithm against
three validation strategies and returns a long-format tibble of
metrics ready for plotting:
\describe{
\item{\verb{in-sample}}{Fit on \code{train}, predict at the same training
monitors. Reproduces the legacy RMSE figure but is biased
downward.}
\item{\code{spatial-cv}}{\code{k}-fold spatial cross-validation on \code{train}
using \code{\link[=spatial_kmeans_folds]{spatial_kmeans_folds()}}. Genuine spatial extrapolation
error.}
\item{\code{held-out}}{Fit on \code{train}, predict at every station in
\code{holdout}. Captures background-to-roadside generalisation.}
}
}
\details{
Results across all three strategies are returned in one place so
the R Journal article's headline figure (RMSE, bias, r broken
down by algorithm and strategy) can be reproduced from a single
object.
}
