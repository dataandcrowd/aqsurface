% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{spatial_block_folds}
\alias{spatial_block_folds}
\title{Spatial block folds based on a regular grid}
\usage{
spatial_block_folds(stations, k = 5, block_size_m = 5000, seed = NULL)
}
\arguments{
\item{stations}{An \code{sf} POINT layer (typically the training
monitors) or a \code{data.frame} with columns \code{X} and \code{Y}.}

\item{k}{Number of folds. Defaults to 5; set lower for small
networks (<= 30 stations).}

\item{block_size_m}{Block edge length in metres. Default 5000.}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A list of \code{k} folds, same structure as
\code{\link[=spatial_kmeans_folds]{spatial_kmeans_folds()}}.
}
\description{
Divides the bounding box of \code{stations} into a regular grid of
\code{block_size_m} metre cells, assigns each station to its cell, and
then groups cells into \code{k} roughly balanced folds via cyclic
assignment. Useful when the user wants tight control over the
physical scale of the held-out blocks.
}
