% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/road_ratio.R
\name{tidy_ratio_table}
\alias{tidy_ratio_table}
\title{Reshape the legacy ratio table into a per-target tibble}
\usage{
tidy_ratio_table(ratio_df, targets = NULL)
}
\arguments{
\item{ratio_df}{Legacy ratio data frame.}

\item{targets}{Optional character vector to subset and reorder rows
(also expected in the zero-padded form, as returned by
\code{\link[=decade_columns]{decade_columns()}}).}
}
\value{
Tibble with columns \code{target}, \code{Back.Road.Ratio},
\code{Back.High.Ratio}.
}
\description{
The legacy \code{pm10.win.ratio} table is keyed by \code{Dates} strings, but
it uses single-digit days (\code{pm10_1_5_day}) while the matching data
columns use two-digit zero-padded days (\code{pm10_1_05_day}). This
helper normalises \code{Dates} to the zero-padded form and selects the
columns that \code{\link[=apply_road_ratio]{apply_road_ratio()}} expects.
}
