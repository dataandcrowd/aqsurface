% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{load_pollutant}
\alias{load_pollutant}
\title{Load a pollutant slice as an \code{sf} POINT layer}
\usage{
load_pollutant(
  path,
  season = c("winter", "summer"),
  station_type = c("bk", "rd"),
  stations = NULL,
  crs = 5181
)
}
\arguments{
\item{path}{Path to \code{pm10.RData} or \code{no2.RData}.}

\item{season}{\code{"winter"} or \code{"summer"}.}

\item{station_type}{\code{"bk"} (background / fixed) or \code{"rd"} (road-side).}

\item{stations}{Optional station \code{sf} from \code{\link[=make_station_sf]{make_station_sf()}} used to
join attributes such as \code{Road_Dist} and \code{DEM}. If \code{NULL} only the
columns present in the RData object are returned.}

\item{crs}{EPSG code or CRS object; defaults to \code{5181} (Korea Central
Belt 2000), which matches the legacy archive.}
}
\value{
An \code{sf} object with the original day/night columns preserved
and an active POINT geometry.
}
\description{
Convenience wrapper around \code{\link[=read_pollutant_rdata]{read_pollutant_rdata()}} that returns a single
season + station-type slice as an \code{sf} object joined with station
attributes. This is the typical input for \code{\link[=fit_uk]{fit_uk()}} or \code{\link[=fit_gam]{fit_gam()}}.
}
